## 2025-05-14 - Loading states in async buttons
**Learning:** Adding a generic `isLoading` prop to foundational UI components like `Button` is a highly efficient way to implement consistent, accessible loading feedback across the app. It ensures that the spinner is consistently styled and the button is automatically disabled to prevent double-submissions.
**Action:** Always check if core UI components support loading states before implementing them manually in page components.
